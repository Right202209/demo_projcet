#include <iostream>
#include <algorithm>
using namespace std;
int main()
{
    int a, b, t;
    cin >> t;
    while (t--)
    {
        scanf("%d+%d",&a,&b);
        printf("%d\n",a+b);
    }
    system("pause");
    return 0;
}
